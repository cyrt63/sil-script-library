/*
Call this script from a workflow transition post-function.
It will set the parent field value to match the issue field value.
*/

if(!isNull(parent)) {
   parent.affectedVersions = affectedVersions;
}
