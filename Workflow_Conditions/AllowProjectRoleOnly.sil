/*
Use this script as a workflow transition condition to allow transition only if
the user invoking the transition is in project role Administrators
*/

string ROLE = "Administrators";
return isUserInRole(currentUser(), project, ROLE);
