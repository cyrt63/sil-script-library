/*
Use this script as a workflow transition validator to allow transition only if
a comment has been supplied.
*/

string errorMsg = "Attachments: " + attachments;
if(!hasInput("fruit")) {

    if(size(attachments) == 0) {
       return false, "comment", errorMsg;
    }
   return false, "comment", errorMsg;
}
