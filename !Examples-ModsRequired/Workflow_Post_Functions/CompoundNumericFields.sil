/*
Call this script from a workflow transition post-function.
It will add values of two numeric fields and set a numeric custom field.

The user is expected to modify the following fields to match their Jira
instance:
* "Urgency" (customField)
* "Impact" (customField)
* "priorityId" (customField)
*/

Urgency = Impact + priorityId;
