/*
Call this script from a workflow transition post-function.
It will save both values of a cascade field into a text field in format
Value1|Value2

The user is expected to modify the following fields to match their Jira
instance:
* customfield_10100 (customField)
* Cascade (customField)
*/

customfield_10100 = Cascade;
